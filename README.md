# TP8 - Banc MCC-MS

Ce depot contient le notebook du TP 8 pour le traitement des donnees et la generation des figures.

## Lancer avec Binder



## Lancer en local

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
jupyter lab
```
